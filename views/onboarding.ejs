<%- include('partials/header') %>

<style>
  .onboarding-container {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 20px;
  }
  .onboarding-card {
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(16px);
    border-radius: 24px;
    padding: 2.5rem;
    max-width: 600px;
    margin: 0 auto;
  }
  .step-indicator {
    display: flex;
    justify-content: center;
    margin-bottom: 2rem;
  }
  .step-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.3);
    margin: 0 6px;
  }
  .step-dot.active {
    background: #6a11cb;
  }
  .step-content {
    text-align: center;
  }
  .step-content h2 {
    font-size: 2rem;
    margin-bottom: 1.5rem;
    color: white;
  }
  .step-content p {
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: 2rem;
    font-size: 1.1rem;
  }
  .avatar-preview {
    font-size: 3rem;
    margin: 0 auto 1.5rem;
    width: 100px;
    height: 100px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 50%;
  }
</style>

<div class="onboarding-container">
  <div class="onboarding-card">
    <div class="step-indicator">
      <div class="step-dot active"></div>
      <div class="step-dot"></div>
      <div class="step-dot"></div>
    </div>
    
    <div class="step-content">
      <div class="avatar-preview"><%= user.emoji %></div>
      <h2>Selamat Datang, @<%= user.username %>!</h2>
      <p>Kamu hampir siap! Ikuti 3 langkah cepat ini untuk memaksimalkan Wanz Req.</p>
      
      <form action="/onboarding" method="POST">
        <button type="submit" class="btn btn-primary btn-lg px-4 py-2">
          <i class="fas fa-arrow-right me-2"></i> Mulai Sekarang
        </button>
      </form>
    </div>
  </div>
</div>

<%- include('partials/footer') %>